<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog">

    <changeSet id="002-users" author="thanh">
        <sql>
            CREATE TABLE sugar_advisor.users (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
                name VARCHAR(100) NOT NULL,
                age INT,
                weight DECIMAL(5,2),
                daily_sugar_limit DECIMAL(6,2),
                created_at TIMESTAMP DEFAULT NOW(),
                updated_at TIMESTAMP DEFAULT NOW()
            );
        </sql>

        <rollback>
            DROP TABLE IF EXISTS sugar_advisor.users;
        </rollback>
    </changeSet>

</databaseChangeLog>